export enum UserRole {
  ADMIN = 'ADMIN',
  FIELD_AGENT = 'FIELD_AGENT'
}

// Move these here too so they are central
export enum FieldStage {
  PLANTED = 'PLANTED',
  GROWING = 'GROWING',
  READY = 'READY',
  HARVESTED = 'HARVESTED'
}